// Grab all external IPs from Google Cloud and turn into nmap command.
```js
let s = ""; 
[...document.querySelectorAll('.cfc-table-cell-container-for-col-1')].map(x => x.innerText).forEach(y => s += y + " "); 
console.log("nmap -A", s);
```
